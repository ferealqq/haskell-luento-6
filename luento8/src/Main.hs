module Main where

-- stack repl --package containers
-- data Suhde = Blokattu | Seurattau deriving (Show)
-- import qualified Data.Map as Map
-- type Tunnus = Text
-- "Kissa" :: Tunnus 
-- data Käyttää = Käyttäjä {suhde :: Map Tunnus Suhde} deriving Show
-- esimK = Käyttäjä {suhde :: Map Tunnus Suhde} deriving show
-- esimK = Käyttäjä (Map.fromList [("pekka",Blokattu),("jukka",Seurattu)])

main :: IO ()
main = do
  putStrLn "Hello TIEA341"
